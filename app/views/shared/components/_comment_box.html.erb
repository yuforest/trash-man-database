<div>
  <% if user_signed_in? %>
    <%= form_with model: [:user, comment.post, comment] do |f| %>
      <div class="form-group">
        <%= f.label :content %>
        <%= f.text_area :content, class: 'form-control' %>
      </div>
      <%= f.submit '送信', class: 'btn btn-primary' %>
    <% end %>
  <% else %>
    <div>
      コメントを書き込むには<%= link_to "会員登録", new_user_registration_path %>や<%= link_to "ログイン", new_user_session_path %>が必要です。
    </div>
  <% end %>
</div>